# 双 OTA 工程学习路线（从零到看懂）

> 适用：会 STM32 / ESP32 基础语法，但看不懂"多任务 + OTA + 网络"工程的人。
> 总时长约 7~9 小时，按顺序做，每阶段末尾有验证动作，**全部打勾才算过**。

---

## 0. 先说结论：你"看不懂"缺的是这 4 个概念

| 概念 | 为什么是这个项目的拦路虎 | 对应代码 |
|---|---|---|
| ① FreeRTOS 多任务 | 4 个 `while(1)` 同时在跑，不是顺序执行 | `main/main.c` |
| ② OTA 双槽机制 | 固件不是覆盖式更新，是"写另一槽+切换" | `partitions.csv`、`main/ota.c` |
| ③ HTTP 客户端/服务器 | ESP32 与电脑的"对话语言" | `main/http_client.c`、`server/server.js` |
| ④ STM32 ROM Bootloader 协议 | 刷 STM32 靠的是芯片出厂自带的 Bootloader，不是你写的程序 | `main/stm32_ota.c` |

你已有的 STM32/ESP32 语法（GPIO、UART、HAL、ESP-IDF API）都是零散的"单词"，
缺的是把这些词串起来的"句子结构"。下面的路线就是教你句法。

---

## 1. 系统全景图（先记住这张图，30 分钟）

```
┌──────────────┐  WiFi(STA)  ┌────────────────────────┐  UART2 @9600  ┌─────────────────┐
│  电脑 (PC)    │ ──────────► │        ESP32           │ ────────────► │   STM32F103     │
│  node server │ HTTP :8888  │  (中转站 + 自己也能OTA) │  TX=GPIO17    │  (GD32 克隆)     │
│  server.js    │             │  GPIO4→BOOT0            │  RX=GPIO16    │  UART1 115200   │
│  firmware/    │             │  GPIO5→NRST             │  BOOT0/复位   │  被动被刷        │
└──────────────┘             └────────────────────────┘  GPIO4/GPIO5  └─────────────────┘
```

**三个设备 = 三套固件 = 三套版本号（最容易混，先背熟）：**

| 固件 | 版本号存在哪 | 谁比较版本 | 比较的对手 |
|---|---|---|---|
| ESP32 自己 | `sdkconfig.defaults` 的 `CONFIG_APP_PROJECT_VER` | `main/ota.c` | 服务器 `server/firmware/version.json` |
| STM32 | 服务器 `stm32_version.json` + ESP32 的 NVS(`stm32_ota`/`last_ver`) | `main/stm32_ota.c` | NVS 里记录的"上次成功刷写版本" |

**规则只有一个：版本必须严格大于才动作。相等 = 什么都不做（只打一行日志）。**
这条规则是你后面所有"看起来卡死"疑问的总答案。

---

## 2. 阶段 1：读"总指挥" main.c —— 打破顺序执行错觉（1 小时）

### 前置概念（各花 5 分钟查官方文档，不用深入）
- FreeRTOS：任务（task）、`xTaskCreate()`、`vTaskDelay()`、`pdMS_TO_TICKS()`
- ESP-IDF 日志宏：`ESP_LOGI / ESP_LOGW / ESP_LOGE`（带 TAG）
- `esp_err_t`：函数返回值，`ESP_OK=0`，非 0 即错误码，用 `esp_err_to_name()` 打印

### 读什么（文件：`main/main.c`，179 行，从头到尾读一遍）
| 位置 | 内容 | 你要领会的点 |
|---|---|---|
| L74 `app_main` | 入口：打印信息→LED→连 WiFi→建 3 个任务 | **这里同时启动了 4 个"循环"** |
| L105-112 心跳循环 | 2s 一次 LED + 日志 | 它就是第 4 个循环，在 `app_main` 自己里面 |
| L119 `http_test_task` | 每 10s GET `/hello` | 任务=函数+独立栈+无限循环 |
| L144 `ota_task` | 每 60s 查 ESP32 固件更新 | 注意 L148：先等 10s 再开始 |
| L165 `stm32_ota_task` | 每 60s 查 STM32 固件更新 | 三个任务周期 10s/60s/60s 要能脱口而出 |
| L93 `ota_confirm_running_app` | WiFi 连通即标记固件有效 | 先记着，阶段 3 讲 |

### 核心理解（本阶段唯一的难点）
`xTaskCreate` 后，**任务调度器会让 4 个 `while(1)` 轮流跑**（每 tick 切一次）。
所以"服务器挂了"不会让 LED 不闪——任务之间是隔离的。这就是本项目日志里
能看到 4 种节奏同时存在的原因。

### ✅ 验证动作
1. 纸上画 4 个任务的时间线（2s 心跳 / 10s http / 60s OTA ×2），标出每 60s 内各任务各执行几次。
2. 烧录抓日志，对照确认 4 类日志的节奏和你画的一致（心跳 `[heartbeat #N]`、`<- 200:`、`OTA: 检查更新`、`STM32 OTA: 检查更新`）。

---

## 3. 阶段 2：读简单模块 —— 建立"模块=接口+实现"心智（1 小时）

按顺序读，每个都是"先 `.h` 后 `.c`"：头文件是使用说明书，`.c` 是内部实现。

### 3.1 `main/led.c`（20 行，热身）
- 注意 L15 的 `static int level`：**static 局部变量**在多次调用间保留值。
- 读完你要能说出：`led_toggle` 为什么阻塞了半个心跳周期（里面有 `vTaskDelay`）。

### 3.2 `main/wifi.c`（148 行，重点）
**前置概念（查 10 分钟）：** 事件驱动编程、EventGroup（事件组）、`xEventGroupWaitBits`。

**核心模型：WiFi 连上是"等出来的"，不是"调出来的"。**
```
wifi_sta_init() 只负责"注册回调 + 启动"，马上返回
                ↓
之后由系统事件循环驱动 wifi_event_handler()（L31）：
  WIFI_EVENT_STA_START       → 自动调 esp_wifi_connect()
  WIFI_EVENT_STA_DISCONNECTED→ 重连，超 WIFI_STA_MAX_RETRY 次 → 置 FAIL 位
  IP_EVENT_STA_GOT_IP        → 记下 IP → 置 CONNECTED 位
                ↓
wifi_sta_wait_connected() 调 xEventGroupWaitBits() 阻塞等待上述位（L122）
```

### 3.3 `main/http_client.c`（122 行，重点，它是后面 OTA 的地基）
**前置概念（查 15 分钟）：** HTTP 协议（GET、状态码 2xx/4xx、Header、Body）、TCP 连接。

**核心理解（看 L47 的注释，这是作者踩过的坑）：**
不能用 `esp_http_client_perform()`，它内部把 body 读掉了；必须手动三步走：
```
esp_http_client_open()        → 建立连接
esp_http_client_fetch_headers() → 读响应头（含状态码）
esp_http_client_read_response() → 把 body 读进你给的缓冲区
```
**"缓冲区指针"心智模型**：`http_client_get_text_hp(host, port, path, buf, buflen, &status)`
就是"给个地方放结果"。返回后 `buf` 里就是服务器返回的文字，`*status` 是 HTTP 状态码。
整个项目的信息流都是从这种"把数据放进缓冲区"开始的。

### ✅ 验证动作
1. 用浏览器/curl 直接访问 `http://192.168.1.11:8888/hello` 和 `/ota/manifest`，亲眼看看 ESP32 每次 GET 到的字符串长什么样。
2. 改 `main.c` 的 `HTTP_TEST_PERIOD_MS` 为 5000，重烧，确认日志节奏变化——证明你理解了任务周期。

---

## 4. 阶段 3：ESP32 自身 OTA —— 本项目最精华的部分（2 小时）

### 前置概念（必看，先花 20 分钟）
1. **分区表** `partitions.csv`：Espressif Flash 布局。本项目：无 factory 槽，`ota_0` + `ota_1` 各 1.9375MB，`otadata` 记录"下次从哪个槽启动"。
2. **sdkconfig.defaults**：第 13 行允许 HTTP 明文 OTA；第 17 行开启回滚保护。
3. **esp_https_ota API**：`begin / perform / finish / abort`（虽叫 https，配置成 http 也能用）。

### OTA 全流程（画一遍这个时序，然后对照代码找每一步）
```
ota_task (60s 一轮)
  │ 1. GET /ota/manifest ──► 服务器回 {"version":"1.0.1","url":"http://...:8888/ota/firmware.bin"}
  │ 2. semver_cmp(远端, 本地) —— 不大于就 return（什么都不发生！）
  ▼ 3. 有新版：
do_ota(firmware_url)                  [ota.c L127]
  │   esp_https_ota_begin()  打开固件流
  │   esp_https_ota_get_img_desc() 读新固件内嵌版本，再核对一次防反复刷
  │   esp_https_ota_perform() 循环下载+写入"另一个槽"（边下边写，进度 10%...）
  │   esp_https_ota_finish()  校验（损坏会失败）并切换 boot 分区
  ▼ 4. esp_restart() 重启 ──── 注意：不返回！此任务就此消失
新固件首启 ──► ota_confirm_running_app() [main.c L93] ──► 标记 valid，取消回滚
（若没标记且中途崩 → 下次重启 bootloader 自动退回旧槽 = 回滚保护）
```

### 读 `main/ota.c` 的顺序（按重要度排）
| 函数 | 位置 | 重点 |
|---|---|---|
| `semver_cmp` | L42 | 手写版本比较：`strtol` 分段解析 "MAJOR.MINOR.PATCH"。**"看起来卡死"的根源在这里** |
| `json_get_string` | L83 | **手写 JSON 解析**（没引 cJSON 库）。纯 C 指针基本功，建议逐行走一遍，理解 `strstr` 找 `"key"` → 跳过空白冒号 → 拷到引号 |
| `ota_check_and_update` | L197 | 总流程：拉 manifest → 解析 → 比版本 → 更新 |
| `do_ota` | L127 | 下载+写槽+切槽，注意 L149 的"版本相同放弃"防呆 |
| `ota_confirm_running_app` | L62 | PENDING_VERIFY → valid 的回滚状态机 |
| `ota_get_running_version` | L54 | `esp_app_get_description()` 读内嵌版本 |

### ✅ 验证动作（先改服务器，不用烧录）
1. `curl http://192.168.1.11:8888/ota/manifest`，看到 JSON 后，对照 `json_get_string` 手工走一遍"如果我是 ESP32 怎么从这串字里抠出版本号"。
2. 观察设备日志至少 70 秒：确认版本相等时只有一行"已是最新"，**没有下载动作**——理解为什么"没反应=正常"。

---

## 5. 阶段 4：STM32 OTA —— 最难但最值钱的部分（2~3 小时）

### 前置概念（先 30 分钟搞懂硬件，再读代码）
1. **STM32 系统 Bootloader（AN3155）**：F103 出厂自带一段程序。`BOOT0=1` + 复位后，芯片不跑你的 App，而是跑这段程序，等 USART1 上的命令。搜索 "STM32 AN3155" 看官方命令列表。
2. **接线**（`main/stm32_ota.h` L24-29）：GPIO17→STM32 PA10(RX)、GPIO16→PA9(TX)、GPIO4→BOOT0、GPIO5→NRST。**ESP32 是"主动方"，STM32 全程被动。**
3. **这个板子的坑**（`stm32_ota.h` L26）：GD32 克隆的 ROM Bootloader **只在 9600 波特率能同步**，且需要两阶段同步（代码 `boot_sync` L354 和 `baud_probe` L629 都有体现）。标准协议是 115200 单阶段——所以"照标准抄"必失败。

### 核心时序（对照代码走一遍）
```
pins_drive_bootloader()  [L239]   GPIO4(BOOT0)=1 → GPIO5(NRST)低脉冲 100ms → 松开
        ↓ STM32 此刻已进 Bootloader（200ms 后苏醒）
boot_sync()              [L354]   发 0x7F → 等 ACK(0x79)，最多 3 次
boot_erase_pages()       [L413]   帧 [43 BC] → [页数-1][页号列表][XOR校验] 逐页擦（1KB/页）
boot_write_image()       [L433]   分 256B 块：写命令头 → 4字节大端地址 → [N-1][数据][XOR]
                                  每步都等 ACK，每块补到 4 字节对齐
boot_go(0x08000000)      [L489]   发 Go 命令 → STM32 跳转执行新固件
pins_release_and_reboot() [L250]  BOOT0=0 → 再复位 → 从应用启动，打印 "APP vX.Y.Z boot"
```

### AN3155 帧格式（作者实测确认，`stm32_ota.c` L292-295 注释）
| 段 | 内容 | 说明 |
|---|---|---|
| 命令头 | `[cmd][~cmd]` | 命令 + 1's 补码（不是 XOR！克隆芯片实测） |
| 数据段 | `[data...][XOR]` | 数据全部字节异或做校验 |
| 地址 | 4 字节大端 | `addr>>24, >>16, >>8, addr` |

### 读 `main/stm32_ota.c` 的顺序
| 函数 | 位置 | 重点 |
|---|---|---|
| `download_image` | L147 | 下载到堆内存（注意 L177 大小上限检查 + L182 堆余量检查） |
| `nvs_get/set_last_ver` | L105/L128 | NVS 键值存取 = ESP32 的"小闪存数据库"，记上次刷的版本 |
| `pins_drive_bootloader` | L239 | **用代码"按" BOOT0 和复位键** —— 这是本模块的钥匙 |
| `boot_sync` | L354 | 0x7F 同步：发一字节，在返回流里找 0x79（跳过回显） |
| `boot_write_image` | L433 | 理解"发一块→等 ACK→发下一块"的握手节奏 |
| `stm32_ota_check_and_update` | L842 | 总流程 7 步：manifest→版本→下载→进 Bootloader→擦→写→Go→复位→记 NVS |

### 对照看一眼"被刷的靶子"
`stm32/esp32_test/Core/Src/main.c` L96-101：STM32 侧就一个横幅打印
`APP v1.0.0 boot`，其余全是 CubeMX 模板。**它完全没有"被 OTA"的代码**——被刷写全靠
芯片 Bootloader + ESP32 在外面操作引脚。理解这个反差 = 理解本模块本质。

### ✅ 验证动作（本阶段结束的毕业考）
1. 改 `server/firmware/stm32_version.json` 版本号 > NVS 记录值（如 1.0.1），**不动任何 ESP32 代码**。
2. 等 ≥70s，日志应依次出现：`版本检查: NVS=.. 远端=..` → `擦除 N 页` → `写入进度 10%...100%` → `Go 0x08000000` → STM32 复位打印 `APP v1.0.1 boot`。
3. 把版本改回 1.0.0，再等 70s，确认只有一行"已是最新, 跳过"——验证 NVS 记录生效。

---

## 6. 阶段 5：看服务器一端（30 分钟）

文件：`server/server.js`（185 行，node 内置 http/fs，0 依赖）。

读法：不需要懂 JS 细节，**只需要看懂一张路由表**：

| 路径 | 干什么 |
|---|---|
| `GET /hello` | 返回一行文字（给 http_test_task 测连通性） |
| `GET /ota/manifest` | 读 `version.json` + 统计 `firmware.bin` 大小，拼 JSON 返回 |
| `GET /ota/firmware.bin` | 流式发送 ESP32 固件 |
| `GET /ota/stm32_manifest` | 同上，换成 stm32 两件套 |
| `GET /ota/stm32.bin` | 流式发送 STM32 固件 |

**关键领悟**：`url` 是服务器用请求头 `Host` 动态拼的（L87、L134）——所以电脑 IP 变了，
服务器不用改；只要改 `main.c` 的 `LOCAL_HOST` 重烧。

顺便看 `server/deploy_firmware.bat` 和 `deploy_stm32.bat`：发布流程 = 拷 bin + 改版本号。

---

## 7. 阶段 6：完整实操 —— 把三端串起来（1 小时，最重要的阶段）

### 实验 A：日志节奏体检（复习阶段 1）
烧录后抓 70s 日志，确认你能在日志里说出"现在是谁在说话"：
```
[heartbeat #N]  ...        ← app_main 心跳, 2s
<- 200: Hello...           ← http_test_task, 10s
OTA: 检查更新... 已是最新   ← ota_task, 60s
STM32 OTA: ... 跳过        ← stm32_ota_task, 60s
```

### 实验 B：触发 STM32 OTA（复习阶段 4）
按阶段 4 验证动作 1-3 做一遍，观察 9600 波特率下整个擦写过程的日志节奏。

### 实验 C：触发 ESP32 自身 OTA（复习阶段 3）
三处同步改：`sdkconfig.defaults` 版本号 + `server/firmware/version.json` + 重新 build。
烧录后 60s 内应看到 `下载进度 10%...100%` → 重启 → 新固件横幅。
**失败会怎样？** 故意不确认（注释掉 `ota_confirm_running_app`），重启后应回滚旧版本——用一次真实回滚理解回滚保护。

### 实验 D（进阶，可选）：用诊断探针理解 Bootloader 行为
`stm32_ota.c` 里留了 3 个探针函数（`stm32_ota_debug_probe` / `baud_probe` / `proto_probe`），
它们是作者当初调试的"刑侦工具"。临时在 `app_main` 里调一次，看它 D1~D4 逐项验证
"应用在发数据吗 → 复位有效吗 → BOOT0 生效吗 → 0x7F 有人答吗"，体会系统化调试方法。

---

## 8. 常见"卡住"现象速查表（挂在旁边，卡住时先查这里）

| 你看到的日志 | 真实含义 | 处理 |
|---|---|---|
| 只有 `OTA: 检查更新...` + `已是最新`，60s 一行 | **正常！** 版本相等，什么都没发生 | 去改版本号 |
| `local server unreachable: ESP_FAIL` | 服务器没开 / IP 不对 / 防火墙拦了 | `node server.js` + 核对 `main.c` 的 `LOCAL_HOST` |
| `OTA: 本轮未更新 (ESP_FAIL)` | manifest 拉取失败 | curl 服务器 URL 验证 |
| `STM32 刷写失败: ESP_ERR_TIMEOUT` | 同步/命令无 ACK | 查接线、9600、BOOT0 时序；用诊断探针 |
| `write flash ... PermissionError(13)` | 残留 idf_monitor 占着 COM4 | AGENTS.md 里的杀进程命令 |
| 中文日志乱码 `�汾���` | 重定向文件 GBK 问题，**不是 bug** | 用 ASCII 关键词 grep |

---

## 9. 读码方法速记（这个项目的正确打开方式）

1. **永远先看 `.h` 再看 `.c`**：头文件是"说明书"，实现是"零件图"。
2. **先找"主流程函数"**（`*_check_and_update`、`app_main`），顺着它往下走，细节函数先跳过。
3. **一个函数只干一件事**：名字看不懂就去看它调了什么、被谁调。
4. **画图代替记忆**：数据流（manifest→版本→下载→写）和时序（引脚/串口握手）画一遍就通了。
5. **验证靠日志**：每理解一段，就制造一次日志变化（改周期、改版本号），日志骗不了人。
6. **卡住先查 `AGENTS.md`**：这个仓库把全部坑都写在里面了，先读它再查代码。

---

## 10. 最终自测清单（全部做到 = 毕业）

- [ ] 能不看代码画出三设备数据流图 + 三个版本号的存放位置
- [ ] 能说出 main.c 里 4 个循环各自的周期和职责
- [ ] 能解释"版本相等为什么像卡死"
- [ ] 能说出 ESP32 OTA 的完整链路（manifest→下载→切槽→重启→确认）
- [ ] 能说出 STM32 OTA 的完整链路（下载→BOOT0+复位→同步→擦→写→Go→复位）
- [ ] 能说出 AN3155 命令帧的结构和校验方式
- [ ] 能说出 9600 波特率这个"坑"的来龙去脉（为什么不是 115200）
- [ ] 独立完成过一次 STM32 OTA 触发、一次 ESP32 OTA 触发
- [ ] 遇到日志异常时，知道先去查 AGENTS.md 和本文件第 8 节
